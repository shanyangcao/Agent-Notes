# 流式输出（Spring AI）

流式输出就是将过程中一部分一部分的展示出来

### SSE思想

 基于 HTTP 的服务器向客户端单向推送数据的技术规范 / 通信协议  

- 前端（浏览器）和后端建立一个**长连接**（一直连着不挂断）；
- 只有**服务器能主动发消息**给前端，前端只能收、不能反向发；
- 消息是「事件流（Event Stream）」格式，前端能按「事件类型」接收和处理。