#### 大模型（Large Model）：                   

参数规模巨大、能通用理解与生成的深度学习模型

#### LLM（大语言模型）：

专注处理文本理解、生成、对话的大模型

#### Transformer：

大模型基础架构，靠注意力机制实现长文本建模

#### 预训练：

用海量数据先学通用语言规律，再做下游任务

#### 微调（Fine-tune）：

在预训练模型上用小数据适配特定场景

#### Prompt（提示词）：

给模型的输入指令，引导其输出内容

#### 上下文窗口：

模型一次能处理的最大文本长度

#### 参数（Parameter）：

模型学习到的权重，决定能力大小

#### Tokenizer（分词器）：

把文字拆成模型可识别的 token

#### Token：

模型处理的最小语义单元，中文常为字 / 词

#### 自回归生成：

逐词生成文本，每次基于前面内容预测下一个

#### 涌现能力：

模型规模变大后突然出现的复杂能力

#### 少样本学习：

只给少量示例就能完成新任务

#### 零样本学习：

不给示例，直接靠指令完成任务

#### 思维链（CoT）：

让模型分步推理，提升逻辑能力

#### 对齐（Alignment）：

让模型输出符合人类价值观与意图

#### RLHF（人类反馈强化学习）：

用人类偏好训练模型更安全好用

#### 幻觉（Hallucination）：

模型编造不存在的虚假信息

#### 通用人工智能（AGI）：

具备人类水平通用智能的系统

#### 多模态模型：

同时处理文本、图像、音频等多种数据

#### 扩散模型：

生成图像、视频的主流生成式模型

#### RAG（检索增强生成）：

先检索外部知识再生成，减少幻觉

#### 向量数据库：

存储高维向量，用于快速相似性检索

#### 嵌入（Embedding）：

把文字 / 图像转为数值向量表示

#### 上下文学习：

在对话历史中直接学习任务，不更新参数

#### 指令微调：

用大量指令数据集微调，让模型听懂人话

#### 基座模型：

未经过对齐、仅预训练的原始大模型

#### 对话模型：

优化过交互逻辑、适合聊天的大模型

#### 知识蒸馏：

把大模型能力迁移到小模型，降低成本

#### 量化（Quantization）：

降低参数精度，减少显存占用

#### LoRA（低秩适配）：

轻量级微调方法，只训练少量参数

#### 全参数微调：

更新模型全部参数，效果好但成本高

#### 过拟合：

模型在训练数据表现极好，泛化差

#### 泛化能力：

模型在未见过数据上的表现能力

**训练数据**：

用于模型预训练与微调的文本 / 图像语料

#### 语料清洗：

去除脏数据、重复、低质内容提升效果

#### 算力：

训练模型所需计算资源，如 GPU、NPU

#### GPU：

图形处理器，大模型训练与推理主力硬件

#### TPU：

谷歌专用 AI 芯片，适合大规模训练

#### NPU：

 神经网络处理器，移动端 / 端侧常用

#### 分布式训练：

多卡 / 多机协同训练超大模型

#### 数据并行：

把数据分给多卡，同步更新模型参数

#### 模型并行：

把模型拆到多卡，分别计算不同部分

#### 显存：

显卡内存，决定模型能否跑起来

#### 推理（Inference）：

模型训练完后接收输入产生输出

#### 训练（Training）：

模型从数据中学习优化参数

#### Batch（批次）：

一次训练送入模型的数据量

#### 学习率：

控制参数更新步长，影响训练稳定性

#### 优化器：

调整参数的算法，如 Adam、SGD

#### 损失函数：

衡量模型输出与真实值差距的函数

#### 自监督学习：

无人工标注，利用数据自身结构学习

#### 监督学习：

用带标签数据训练模型

#### 无监督学习：

无标签数据，学习数据内在模式

#### 强化学习：

通过奖励信号不断优化策略

#### 注意力机制：

让模型关注输入中重要部分

#### 多头注意力：

并行多组注意力，捕捉不同语义关系

#### 编码器：

Transformer 中负责理解输入的部分

#### 解码器：

负责生成输出文本的部分

#### Encoder-only：

仅编码器，如 BERT，擅长理解

#### Decoder-only：

仅解码器，如 GPT，擅长生成

#### Seq2Seq：

输入序列到输出序列的生成框架

#### BERT：

经典预训练模型，主打理解与分类

#### GPT：

生成式预训练 Transformer，通用对话生成

#### T5：

统一框架，把所有任务转为文本生成

#### LLaMA：

Meta 开源大语言模型系列

#### 混元：

腾讯自研大模型

#### 端侧部署：

把模型跑在手机、本地设备上

#### 云部署：

模型在云端服务器运行，API 调用

#### API 接口：

程序调用模型能力的标准化入口

#### 吞吐：

单位时间模型处理请求数量

#### 延迟：

从输入到输出的响应时间

#### 温度系数：

控制生成随机性，值越高越发散

#### Top-p 采样：

按概率累加截断，控制生成多样性

#### Top-k 采样：

只选概率最高 k 个词生成

#### 重复惩罚：

避免模型生成重复句子

#### 插件（Plugin）：

让模型调用外部工具如搜索、计算器

#### Agent（智能体）：

能自主规划、调用工具完成任务

#### 工具调用：

模型联网、查数据库、执行代码

#### 代码解释器：

让模型写代码并运行得到结果

#### 长文本理解：

处理书籍、论文等超长内容

#### OCR：

光学字符识别，提取图片文字

#### ASR（语音识别）：

把语音转为文字

#### TTS（语音合成）：

把文字转为自然语音

#### 多轮对话：

保留历史信息，实现连贯交互

#### 记忆机制：

模型记住长期对话与用户偏好

#### 安全对齐：

防止模型生成有害、违法内容

#### 内容审核：

过滤模型输入输出的违规信息

#### 预训练语料：

海量通用文本，如网页、书籍

#### 领域大模型：

面向金融、法律、医疗等垂直领域

#### 小样本微调：

用极少数据快速适配场景

#### 迁移学习：

把一个任务能力迁移到另一任务

#### 模型压缩：

减小模型体积，提升推理速度

#### 剪枝：

移除模型中不重要的参数与神经元

#### 知识图谱：

结构化知识库，辅助模型准确回答

#### 语义理解：

模型读懂文字真实含义与意图

#### 文本生成：

模型自动写文章、代码、对话

#### 开源模型：

公开权重与代码，可自由下载使用

#### Hugging Face：

建设AI开源平台及社区

#### 